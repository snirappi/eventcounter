{
    "scripts": {
        "test": "jest --coverage"
    },
    "devDependencies": {
        "jest": "^26.4.0",
        "jest-date-mock": "^1.0.8"
    },
    "jest": {
        "setupFiles": [
            "jest-date-mock"
        ]
    }
}
